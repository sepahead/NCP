/target
/candidate-dossier/

# TS package
node_modules/
ncp-ts/node_modules/

# Buf preview codegen (not yet the shipped SDKs; see buf.gen.yaml)
/gen

# Python bytecode
__pycache__/
*.pyc

# Deployment identity material (never commit real mTLS credentials)
.env
.env.*
!.env.example
deploy/certs/
*.pem
*.crt
*.cer
*.key
*.p12
*.pfx

# Internal review scratch (not a shipped artifact)
REVIEW_AND_PLAN.md

# OS noise
.DS_Store

# Editor / IDE local-history
.idea/
.vscode/
*.iml
*.swp

# Agent-tool working directories (scratch; commit shared config, ignore local)
.claude/settings.local.json
.playwright-mcp/

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
