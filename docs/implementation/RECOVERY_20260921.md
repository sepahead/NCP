# Local change recovery, 2026-09-21

This record separates recovered implementation from preserved design material.
Recovery does not authorize a release or close an implementation-ledger task.

## Sources

Integration starts from `e61f4d0e90f717d0631addc0802a26c3fd590cce`.
The unpublished snapshot originally targeted `5d6679ce616477501139be717cfe66ade731ff37`.
Its [remote archive](https://github.com/sepahead/NCP/tree/24f4aa094af37fb173858872cd350b2ffc589bac/reviews/local-recovery-20260920)
retains the exact patch and hashes for 89 files.
Patch replay reproduced all 89 file hashes.
Temporary review images remain in the private recovery bundle.

The [earlier review candidate](https://github.com/sepahead/NCP/commit/2287198dd421ab2408f4426bdce1eccc8944cf20)
supplies the dependency-lock update and modular application documentation.
Its interrupted qualification does not transfer to this integration.

## Reconciliation decisions

| Component | Decision and reason |
| --- | --- |
| Rust bounded codecs and dispatch | Recover allocation and queue changes. Preserve newer ingress, sequence-exhaustion, and missing-data checks. |
| Gateway | Recover stricter response handling. Keep delayed failures from retiring a newer session generation. |
| TypeScript client | Recover responder continuity, single-flight mutation, receipt state, and uncertain-outcome retirement. Preserve current horizon regressions. |
| WebSocket client | Recover aggregate pending UTF-8 limits. A bounded individual message does not bound the queue. |
| NEST runner | Recover proportional feedback and stricter causal reply checks. Test controls do not establish a live NEST result. |
| Generated bindings | Regenerate from current Rust sources. Historical generated files do not override the generator. |
| SDK dependency gate | Permit root lock updates while preserving historical SDK bytes and exact current-source mutation checks. |
| Canonical digest helper | Keep current frozen SDK source. The proposed formatting optimization does not justify changing that reference. |
| Modular documentation | Recover independent composition, sensor ownership, and explicit remaining qualification requirements. |
| Mathematical explanation | Recover a focused [causality and stability note](CLOSED_LOOP_MATH.md), with assumptions and worked examples. |
| Publication redesign | Retain in the remote archive. It conflicts with newer figures and the cross-toolchain PDF verification contract. |
| Historical task-ledger edits | Retain as archive input only. Old completion bookkeeping cannot establish current evidence. |

Three separate reviews covered Rust, clients, and documentation.
Their combined lenses included semantics, security, bounded resources, failure recovery, concurrency, compatibility, performance claims, mathematical assumptions, accessibility, and maintenance.
The reviews favored component-level recovery over replacing current files wholesale.

## Verification boundary

Focused controls cover Rust unit and integration behavior, TypeScript vectors and packages, Python runner failures, generated bindings, and Markdown links.
The SDK gate also tests historical-byte drift and source mutation after a root lock update.
These checks grant no installed interoperability, real-time performance, physical safety, scientific, or release qualification.

The complete repository gate must run against the reconciled immutable commit before promotion to `main`.
Its exact result belongs to the corresponding execution record.
Live NEST, external security, final artifact publication, and remaining product-v1 gates remain separate.
