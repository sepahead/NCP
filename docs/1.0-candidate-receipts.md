# NCP 1.0 candidate task receipts

> **Status:** the complete local candidate gate and hosted CI passed for exact
> committed implementation cut `f08c2ad` on 2026-07-14 after the lifecycle/schema
> correction. Release remains blocked. This ledger is time-bound implementation
> evidence, not a release dossier, signature, independent reproduction, or
> substitute for a required external test.

## Candidate identity (exact implementation cut)

| Axis | Value |
|---|---|
| package | `1.0.0-rc.1` |
| stable wire | `1.0` |
| compact proto FNV-1a | current candidate pin `163acc57d8a62b66` |
| complete normative SHA-256 | implementation-cut pin `9cae331742d01e9b164e029aa06c644e6b1886176d0816a6ef883af138355c90` |
| mandatory corpus SHA-256 | implementation-cut pin `83bdcfae2e07f1c69efa87279f0b3c27392be83f31b292647cddd10eb35226b3` |
| RC build identity | `unreleased-worktree` (deliberately non-certifying) |
| migration input | canonical `v0.8.0`, wire `0.8`, compact hash `d1b50a2d8a265276` |

The local run and [hosted CI](https://github.com/sepahead/NCP/actions/runs/29366777050)
both bind source revision `f08c2ad5f68bab0a583db918439660636996ca07`. The
generated identity values above, exact candidate baseline, and complete local gate
match that implementation cut, but no release artifact identity or signed dossier
exists. A future signed artifact manifest must bind the final normative bytes,
source revision, toolchains, environments, packages, SBOM, and signatures.

## Task receipt index

`Local complete` means the repository-owned deterministic scope and documentation
passed at the recorded implementation cut. It does not imply that a
task's live, independent, duration, consumer, multi-platform, or publication
acceptance test passed.

| Task | Candidate receipt | Residual acceptance gap |
|---|---|---|
| A3-00 | **Superseded local receipt.** Exact canonical `v0.8.0` baseline, mirror reference, compatibility inventory, consumer-pin scan, and byte-drift check passed in the recorded snapshot. | Engram's explicit native-1.0 migration now makes the old v0.8 consumer-pin assertion intentionally false; rerun against the completed migration state. |
| A3-01 | **Local complete.** Stable/experimental/excluded surface, registries, precedence, complete contract digest, and bare-`NCPB`/`BulkObservation` exclusions are enforced. | Independent adoption is not inferred. |
| A3-02 | **Partial.** Two closed profiles, config/ACL validators, ephemeral certificate self-test, and downgrade-negative corpus pass. | Stable Zenoh peer-principal binding is unavailable; `open_secure` fails closed, so the live non-loopback mTLS/ACL campaign is **NOT RUN**. |
| A3-03 | **Partial.** Principal/entity/role/plane/session claims, core authenticated-actor decisions, stable denial codes, and negative decisions are implemented. | The Zenoh callback surface cannot supply the authenticated actor for payload binding; implementation and cross-language production-secure evidence remain blocked. |
| A3-04 | **Local complete.** Authority terms, leases, epochs, transfer/conflict, expiry equality, restart, partition, and ESTOP invariants pass deterministic/exhaustive tests. | Cross-process/live-peer certification remains **NOT RUN**. |
| A3-05 | **Local complete.** `request-digest-v1`, authenticated responder receipts, state versions, principal-bound idempotency, replay/conflict/unknown outcome, and restart snapshots pass deterministic tests. | Router/process-restart fault campaign is **NOT RUN**. |
| A3-06 | **Local complete.** Required fields and explicit unknown/fail-closed states are parity-checked against 84 retained safe schema defaults. Nested `SessionRef.generation` and `StreamPosition.epoch/seq` are schema-required; `ErrorFrame.code` remains registry-closed. | Broader live, independent, and external release gates remain **NOT RUN**. |
| A3-07 | **Local complete.** Universal bounded JSON and duplicate-key/resource decisions agree in Rust and independent TypeScript, replay through Python/C ABI, and gate the dependency-free PyNEST JSONL reply reader before generic decoding. | Required duration fuzz/resource campaign is **NOT RUN**. |
| A3-08 | **Local complete.** Four content-addressed plant profiles, safe-action coverage, ESTOP boundary, fail-closed construction, and scientific non-claims pass. | No physical plant is certified. |
| A3-09 | **Local complete.** TTL equality, clock rewind, sequence/epoch, horizon drain, replay, source correlation, and HOLD/ESTOP precedence pass. | Combined live network-fault campaign is **NOT RUN**. |
| A3-10 | **Local complete.** The generated manifest requires exactly 282 vectors (275 stable, 7 migration), 13 normative requirements, exact execution sets, and zero silent skips. | Signed external conformance reports are **NOT RUN**. |
| A3-11 | **Partial.** TypeScript independently implements bounded parsing, validation, digesting, client correlation, safety, and an experimental WebSocket binding with 19 deterministic fake-socket scenarios, including bounded binary replies, FIFO-terminal errors, queue capacity, outbound byte limits, and finite phase/request deadlines. | Two installed independent non-Rust production-secure peers and the full live matrix are **NOT RUN**. |
| A3-12 | **BLOCKED.** Structural ACL/profile and ephemeral certificate self-tests are only prerequisites; the reference Zenoh adapter deliberately refuses `production-secure` startup without callback-visible authenticated peer identity. | Implement and negative-test the binding, then run live mTLS identity, ACL, certificate validity, rotation, revocation, permissions, and cross-language evidence. |
| A3-13 | **Partial.** Plane-specific bounded queues and deterministic loss/replay/restart/idempotency tests pass, including real local TCP Zenoh happy/negative paths. | Combined delay/loss/reorder/partition/restart/flood, metrics, and duration soak are **NOT RUN**. |
| A3-14 | **Partial.** Boundary/negative corpus, bounded parsers, panic containment, FFI null/ownership checks, and differential language replay pass. | Coverage-guided duration fuzzing, sanitizer matrix, threaded FFI stress, and retained coverage reports are **NOT RUN**. |
| A3-15 | **Partial.** Closed event/metric registries, actor-bound canonical audit records, redaction bounds, and mutation/deletion/reorder chain detection pass in Rust. | Cross-language emission, production retention, signing/external anchoring, and trace campaign are **NOT RUN**. |
| A3-16 | **NOT RUN.** Historical/informative measurements are excluded from release evidence. | Release-bound secure/insecure profiles, tails, overload, memory, cross-language runs, raw data, and regression thresholds remain release-blocking. |
| A3-17 | **Time-bound partial.** Five clean Rust archives, an installed abi3 wheel, root/nested npm tarballs, C++ demo, runtime identity, local conformance, dependency policy, and protobuf gates passed for exact implementation cut `f08c2ad`. | Immutable multi-OS builds, sdist/ABI matrix, registry namespace ownership, reproducibility, artifact hashes, vulnerability report, SBOM, signatures, and provenance are **NOT RUN**. The intended crates.io `ncp-core` and PyPI `ncp` names currently identify unrelated projects. |
| A3-18 | **Partial.** A labelled authenticated terminating translator implements the only safe 0.8 mapping currently supported; missing/malformed/mixed/unsafe context and transparent/reverse translation fail closed across seven vectors. | Full installed 0.8↔1.0 lifecycle, retry, security, fault/backpressure proof, and deprecation evidence are **NOT RUN**. |
| A3-19 | **BLOCKED.** A candidate baseline and time-bound local matrix exist; no tag or publication was made. | Required pre-release gates, signed dossier, independent reproduction, and all six consumer certifications precede publication; install smoke and emergency-revocation validation run only after publication. |

## Retained local evidence

- The 2026-07-14 `scripts/check.sh` pass at `f08c2ad` covered formatting, Clippy,
  workspace tests, generation, installed packages, C++ demo,
  bounded pure-Python JSONL ingress, corpus/schema/proto/digest/baseline,
  security/profile validators, archives, dependency policy, and Buf. The lock used
  non-yanked `spin` 0.9.9/0.10.1. Hosted CI run `29366777050` passed the exact
  source SHA. This is complete source-bound local/hosted preflight evidence, but
  without the required signed artifacts and external gates it is not a release
  receipt.
- Current Rust evidence: 266 base `ncp-core` unit tests plus 47 feature-gated
  TypeScript binding exporters, 21 C ABI unit tests, 35 Zenoh unit tests, 17 gateway
  unit tests, exact Rust/C behavior and conformance suites, and all four real local
  Zenoh integration tests passed. These local transports do not
  exercise `production-secure` identity binding or satisfy A3-12.
- Independent TypeScript evidence: 231 behavioral vectors, 14 canonical messages,
  client/safety checks, 19 deterministic WebSocket scenarios, and two
  installed tarball forms.
- Installed Python abi3-wheel evidence: 231 behavioral vectors, 14 canonical
  messages, zero manifest skips, and 28 smoke tests.
- C/C++ evidence: 21 ABI unit tests, 5 behavior groups, 5 exact corpus/digest tests,
  and a compiled runtime-identity/safety demo.
- Pure-Python E2E evidence: 20 tests comprise 5 hostile/positive bounded-ingress
  groups covering LF/CRLF, byte caps, truncation, strict UTF-8 and surrogate
  handling, decoded duplicate keys, nesting, numeric, and string budgets before
  `json.loads`, plus 15 fail-closed runner-status tests.
- The candidate `conformance/baseline/v1.0.0` snapshot was regenerated after the
  repairs and exact-matches the current 14 schemas/vectors. This is a candidate
  audit snapshot, never an immutable release or compatibility promise.
- Engram's vendored mirror passed its exact v0.8 drift check in that historical
  snapshot. Its explicit native-1.0 migration is now in progress and intentionally
  invalidates the old pin assertion; the native five-NEST runner remains **NOT RUN**
  pending retained end-to-end evidence, and no coercing fallback is permitted.

The authoritative release verdict and external blocker list remain in
[`../RELEASE_READINESS.md`](../RELEASE_READINESS.md).
